# 更新日志（Changelog）

本项目所有显著变更都记录在本文件中。
格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

> **版本对应关系**：项目开发期内部使用功能方案文档的迭代编号（v1.1 – v1.18），
> 对外发布标签按语义化版本重新映射，对应关系如下：
>
> | 发布标签 | 内部迭代 | 内容 |
> | --- | --- | --- |
> | v0.1.0 | v1.5 – v1.6 | 工程骨架 + M1 核心闭环 |
> | v0.2.0 | v1.7 – v1.9 | M2 社区页面 |
> | v0.3.0 | v1.10 – v1.12 | 真机稳定性专项 |
> | v0.4.0 | v1.13 – v1.14 | 成绩表 + 编辑器体验专项 |
> | v0.5.0 | v1.15 – v1.16 | 正式包 + 字体系统扩充 |
> | v0.6.0 | v1.17 | 权限修复 + 错误文案 |
> | v1.0.0 | v1.18 | 编辑器二期收官 |
>
> 开发周期：2026 年 9 月。

## [1.0.0] - 2026-09

### 新增
- **编辑器撤销/重做**：快照栈（上限 100 条），打字/退格按光标连续性自动合并为一条记录，粘贴等多字符编辑独立成条；底栏撤销/重做按钮随栈状态自动启停；支持硬件键盘 Ctrl+Z / Ctrl+Y / Ctrl+Shift+Z。
- **编辑器查找替换**：大小写敏感开关、上/下一个匹配跳转、逐个替换与全部替换；匹配高亮画在 Canvas 装饰层，不干扰文本管线。
- **头像兜底**：真实头像加载失败时回落为「用户名首字母 + 容器色」色块，gravatar 域名不可达的用户不再显示空白。

### 质量
- 离线自检扩到 28 组 497 项全绿；变异探针 10 种全部捕获；字节码 ABI 体检 590 class 通过；debug/release 双构建成功并互相覆盖安装验证。

## [0.6.0] - 2026-09

### 修复
- **普通用户看不了已参加竞赛的题目**：竞赛题是隐藏题（`hidden=true`），请求详情必须携带竞赛上下文 `tid`；题目详情、编辑器、提交、自测全链路补齐该参数。

### 新增
- **错误文案人话化**：按错误 `name` 分类翻译技术错误（如 403 权限错误），未识别的原样透传，绝不编造；网络层 `Failure` 同时保留站点原文供诊断。

## [0.5.0] - 2026-09

### 新增
- **正式发布包（release）**：配置正式签名，产出可覆盖升级的 release APK。
- **启动图标重制**：由设计稿 `icon.ico` 拆分前景/底色/单色三层生成自适应图标，像素画用 NEAREST 整数倍放大，圆形/方圆/方形遮罩均不缺角。
- **内置字体扩充到 6 款**：系统等宽 + Cascadia Mono（可变）/ Cascadia Code / JetBrains Mono / Fira Code / Source Code Pro，共 9 个 TTF；全部为可合法再分发的开源字体，OFL 许可正文逐字随包分发（`res/raw/*_notice.txt`）。

### 修复
- 两处离线构建阻断：Kotlin DSL 中 `java.util.Properties` 需显式导入；离线缓存缺失 lint 产物导致 `assembleRelease` 卡死（以 `checkReleaseBuilds=false` 规避，`:app:lint` 仍可单独执行）。

## [0.4.0] - 2026-09

### 新增
- **竞赛成绩表**：站点渲染矩阵解析，本人行高亮，每 10 秒静默刷新（失败不清空已有数据），带记录的格子可跳评测详情；成绩判定色由 App 端按分数表达，**刻意不解析站点下发的明文邮箱**。
- **编辑器自动补全**：候选 = 语言保留字 + 内建函数 + 当前代码标识符（按出现次数加权）；弹层锚定光标行、宽度自适应；硬件键盘 ↑↓/Enter/Tab/Esc 完整可用。
- **编辑动作引擎**：自动补配对括号、换行继承缩进、跳过已存在的右括号、手打 `}` 退回缩进、成对删除、选区包裹。
- **符号条**：把只有硬件键盘才有的编辑能力（Tab、各类括号、引号）暴露给软键盘用户。
- **代码字体系统**：Cascadia Mono（可变字体三档字重）+ 字号 10–22sp，编辑器与只读视图共用，统一在设置页配置。
- **视觉增强**：配对括号高亮、当前行左侧竖线（Canvas 装饰层，只加样式不改文本长度）。

### 修复
- `getCursorRect` 在布局落后一帧时按文本长度钳制，修复快速删减文本时的越界崩溃。

## [0.3.0] - 2026-09

### 修复
- **登录成功却显示未登录**：Hydro 的 `/api/user` 从不下发 `authn` 字段（那是 WebAuthn 属性），登录判定改为 `_id > 0 && role != "guest"`。
- **排行榜点进用户主页看到全站记录**：提交记录过滤参数实为 `uidOrName`，传 `uid` 会被服务端静默忽略。
- **作业认领无效**：认领走 `POST /homework/:tid + operation=attend`（空 body POST 返回 405），题目列表从 `pdict` 取而非讨论区 `ddocs`。
- 两类启动崩溃：依赖冲突（`listenablefuture`）引发的 VerifyError；竞赛/作业列表 `docId` 缺失导致 LazyColumn key 重复崩溃。

### 新增
- **竞赛题目列表 A~Z 标号 + 题目名**（26 进制标号与站点行为一致）。
- **编辑器题面顶栏**：下拉展开题面、上拉收起，Markdown 渲染。
- **自测（pretest）**：自定义输入组逐组运行，`message` 即程序 stdout，不计提交数、不进记录列表；退避轮询与正式提交一致。
- 登录表单补 `rememberme`；CookieJar 修复 hostOnly/httpOnly 标志位编解码错位。

### 质量
- 完成一次 App 内真实提交（AC），提交/轮询契约全部实测闭环；1000 次 monkey 零 FATAL。

## [0.2.0] - 2026-09

### 新增
- **排行榜**与**用户主页**（含头像加载）。
- **提交记录列表**：全站 / 本人 / 某题目三种范围，区分「没有记录」与「契约不符」。
- **竞赛与作业**：列表、详情、阶段徽标（未开始/进行中/已结束由起止时间客户端推算）。
- **评测详情页查看已提交代码**：复用编辑器高亮，可复制。
- 登录成功返回后，登录墙后的页面自动重载。

## [0.1.0] - 2026-09

### 新增
- 首个可安装版本。Kotlin + Jetpack Compose（Material 3）单 Activity 架构，全程命令行可离线构建。
- **登录**、**题库列表**、**题目详情**、**设置**（14 款内置主题，主色目录清单驱动）。
- **题干 Markdown 渲染器**：标题/粗体/行内代码/公式占位，纯自研解析。
- **代码编辑器**：语法高亮（注释/字符串/数字/关键字/内建五类）、本地草稿自动保存。
- **提交与评测**：提交代码、评测结果退避轮询（封顶 90s）、判题状态色与主题解耦。
- **网络层**：恒发 `Accept: application/json`，四形态响应分发（正常 / 登录软跳转 / 错误信封 / 传输异常），「200 也可能是错误」。

[1.0.0]: # 保留占位，如托管后请替换为 compare 链接
